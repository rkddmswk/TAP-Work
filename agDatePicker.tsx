components > agGrid > components > renderer > agDatePicker
==============================================================

1) gridField 설정
gridField.date({
  headerName: 'Period From',
  field: 'periodFrom',
  width: 150,
  validationKey: true,
  vallu
})


2) srv
